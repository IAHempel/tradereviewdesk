{
  "name": "tradenoc",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w web",
    "build:web": "npm run build -w web",
    "lint:web": "npm run lint -w web",
    "check:web": "npm run typecheck -w web",
    "test:api": ".\\.venv\\Scripts\\python.exe -m unittest discover -s apps/api/tests -v"
  }
}
